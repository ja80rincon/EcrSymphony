// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/counter"
	"github.com/facebookincubator/symphony/pkg/ent/counterformula"
	"github.com/facebookincubator/symphony/pkg/ent/formula"
)

// CounterFormulaCreate is the builder for creating a CounterFormula entity.
type CounterFormulaCreate struct {
	config
	mutation *CounterFormulaMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (cfc *CounterFormulaCreate) SetCreateTime(t time.Time) *CounterFormulaCreate {
	cfc.mutation.SetCreateTime(t)
	return cfc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (cfc *CounterFormulaCreate) SetNillableCreateTime(t *time.Time) *CounterFormulaCreate {
	if t != nil {
		cfc.SetCreateTime(*t)
	}
	return cfc
}

// SetUpdateTime sets the update_time field.
func (cfc *CounterFormulaCreate) SetUpdateTime(t time.Time) *CounterFormulaCreate {
	cfc.mutation.SetUpdateTime(t)
	return cfc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (cfc *CounterFormulaCreate) SetNillableUpdateTime(t *time.Time) *CounterFormulaCreate {
	if t != nil {
		cfc.SetUpdateTime(*t)
	}
	return cfc
}

// SetMandatory sets the mandatory field.
func (cfc *CounterFormulaCreate) SetMandatory(b bool) *CounterFormulaCreate {
	cfc.mutation.SetMandatory(b)
	return cfc
}

// SetFormulaID sets the formula edge to Formula by id.
func (cfc *CounterFormulaCreate) SetFormulaID(id int) *CounterFormulaCreate {
	cfc.mutation.SetFormulaID(id)
	return cfc
}

// SetNillableFormulaID sets the formula edge to Formula by id if the given value is not nil.
func (cfc *CounterFormulaCreate) SetNillableFormulaID(id *int) *CounterFormulaCreate {
	if id != nil {
		cfc = cfc.SetFormulaID(*id)
	}
	return cfc
}

// SetFormula sets the formula edge to Formula.
func (cfc *CounterFormulaCreate) SetFormula(f *Formula) *CounterFormulaCreate {
	return cfc.SetFormulaID(f.ID)
}

// SetCounterID sets the counter edge to Counter by id.
func (cfc *CounterFormulaCreate) SetCounterID(id int) *CounterFormulaCreate {
	cfc.mutation.SetCounterID(id)
	return cfc
}

// SetNillableCounterID sets the counter edge to Counter by id if the given value is not nil.
func (cfc *CounterFormulaCreate) SetNillableCounterID(id *int) *CounterFormulaCreate {
	if id != nil {
		cfc = cfc.SetCounterID(*id)
	}
	return cfc
}

// SetCounter sets the counter edge to Counter.
func (cfc *CounterFormulaCreate) SetCounter(c *Counter) *CounterFormulaCreate {
	return cfc.SetCounterID(c.ID)
}

// Mutation returns the CounterFormulaMutation object of the builder.
func (cfc *CounterFormulaCreate) Mutation() *CounterFormulaMutation {
	return cfc.mutation
}

// Save creates the CounterFormula in the database.
func (cfc *CounterFormulaCreate) Save(ctx context.Context) (*CounterFormula, error) {
	var (
		err  error
		node *CounterFormula
	)
	cfc.defaults()
	if len(cfc.hooks) == 0 {
		if err = cfc.check(); err != nil {
			return nil, err
		}
		node, err = cfc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*CounterFormulaMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = cfc.check(); err != nil {
				return nil, err
			}
			cfc.mutation = mutation
			node, err = cfc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(cfc.hooks) - 1; i >= 0; i-- {
			mut = cfc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, cfc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (cfc *CounterFormulaCreate) SaveX(ctx context.Context) *CounterFormula {
	v, err := cfc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (cfc *CounterFormulaCreate) defaults() {
	if _, ok := cfc.mutation.CreateTime(); !ok {
		v := counterformula.DefaultCreateTime()
		cfc.mutation.SetCreateTime(v)
	}
	if _, ok := cfc.mutation.UpdateTime(); !ok {
		v := counterformula.DefaultUpdateTime()
		cfc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (cfc *CounterFormulaCreate) check() error {
	if _, ok := cfc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := cfc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := cfc.mutation.Mandatory(); !ok {
		return &ValidationError{Name: "mandatory", err: errors.New("ent: missing required field \"mandatory\"")}
	}
	return nil
}

func (cfc *CounterFormulaCreate) sqlSave(ctx context.Context) (*CounterFormula, error) {
	_node, _spec := cfc.createSpec()
	if err := sqlgraph.CreateNode(ctx, cfc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (cfc *CounterFormulaCreate) createSpec() (*CounterFormula, *sqlgraph.CreateSpec) {
	var (
		_node = &CounterFormula{config: cfc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: counterformula.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: counterformula.FieldID,
			},
		}
	)
	if value, ok := cfc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: counterformula.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := cfc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: counterformula.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := cfc.mutation.Mandatory(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeBool,
			Value:  value,
			Column: counterformula.FieldMandatory,
		})
		_node.Mandatory = value
	}
	if nodes := cfc.mutation.FormulaIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   counterformula.FormulaTable,
			Columns: []string{counterformula.FormulaColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: formula.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := cfc.mutation.CounterIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   counterformula.CounterTable,
			Columns: []string{counterformula.CounterColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: counter.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// CounterFormulaCreateBulk is the builder for creating a bulk of CounterFormula entities.
type CounterFormulaCreateBulk struct {
	config
	builders []*CounterFormulaCreate
}

// Save creates the CounterFormula entities in the database.
func (cfcb *CounterFormulaCreateBulk) Save(ctx context.Context) ([]*CounterFormula, error) {
	specs := make([]*sqlgraph.CreateSpec, len(cfcb.builders))
	nodes := make([]*CounterFormula, len(cfcb.builders))
	mutators := make([]Mutator, len(cfcb.builders))
	for i := range cfcb.builders {
		func(i int, root context.Context) {
			builder := cfcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*CounterFormulaMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, cfcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, cfcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, cfcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (cfcb *CounterFormulaCreateBulk) SaveX(ctx context.Context) []*CounterFormula {
	v, err := cfcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
