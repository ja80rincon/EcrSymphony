// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/formula"
	"github.com/facebookincubator/symphony/pkg/ent/networktype"
)

// NetworkTypeCreate is the builder for creating a NetworkType entity.
type NetworkTypeCreate struct {
	config
	mutation *NetworkTypeMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (ntc *NetworkTypeCreate) SetCreateTime(t time.Time) *NetworkTypeCreate {
	ntc.mutation.SetCreateTime(t)
	return ntc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (ntc *NetworkTypeCreate) SetNillableCreateTime(t *time.Time) *NetworkTypeCreate {
	if t != nil {
		ntc.SetCreateTime(*t)
	}
	return ntc
}

// SetUpdateTime sets the update_time field.
func (ntc *NetworkTypeCreate) SetUpdateTime(t time.Time) *NetworkTypeCreate {
	ntc.mutation.SetUpdateTime(t)
	return ntc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (ntc *NetworkTypeCreate) SetNillableUpdateTime(t *time.Time) *NetworkTypeCreate {
	if t != nil {
		ntc.SetUpdateTime(*t)
	}
	return ntc
}

// SetName sets the name field.
func (ntc *NetworkTypeCreate) SetName(s string) *NetworkTypeCreate {
	ntc.mutation.SetName(s)
	return ntc
}

// AddFormulaNetworkTypeFKIDs adds the formulaNetworkType_FK edge to Formula by ids.
func (ntc *NetworkTypeCreate) AddFormulaNetworkTypeFKIDs(ids ...int) *NetworkTypeCreate {
	ntc.mutation.AddFormulaNetworkTypeFKIDs(ids...)
	return ntc
}

// AddFormulaNetworkTypeFK adds the formulaNetworkType_FK edges to Formula.
func (ntc *NetworkTypeCreate) AddFormulaNetworkTypeFK(f ...*Formula) *NetworkTypeCreate {
	ids := make([]int, len(f))
	for i := range f {
		ids[i] = f[i].ID
	}
	return ntc.AddFormulaNetworkTypeFKIDs(ids...)
}

// Mutation returns the NetworkTypeMutation object of the builder.
func (ntc *NetworkTypeCreate) Mutation() *NetworkTypeMutation {
	return ntc.mutation
}

// Save creates the NetworkType in the database.
func (ntc *NetworkTypeCreate) Save(ctx context.Context) (*NetworkType, error) {
	var (
		err  error
		node *NetworkType
	)
	ntc.defaults()
	if len(ntc.hooks) == 0 {
		if err = ntc.check(); err != nil {
			return nil, err
		}
		node, err = ntc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*NetworkTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ntc.check(); err != nil {
				return nil, err
			}
			ntc.mutation = mutation
			node, err = ntc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ntc.hooks) - 1; i >= 0; i-- {
			mut = ntc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ntc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ntc *NetworkTypeCreate) SaveX(ctx context.Context) *NetworkType {
	v, err := ntc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (ntc *NetworkTypeCreate) defaults() {
	if _, ok := ntc.mutation.CreateTime(); !ok {
		v := networktype.DefaultCreateTime()
		ntc.mutation.SetCreateTime(v)
	}
	if _, ok := ntc.mutation.UpdateTime(); !ok {
		v := networktype.DefaultUpdateTime()
		ntc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ntc *NetworkTypeCreate) check() error {
	if _, ok := ntc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := ntc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := ntc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := ntc.mutation.Name(); ok {
		if err := networktype.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (ntc *NetworkTypeCreate) sqlSave(ctx context.Context) (*NetworkType, error) {
	_node, _spec := ntc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ntc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (ntc *NetworkTypeCreate) createSpec() (*NetworkType, *sqlgraph.CreateSpec) {
	var (
		_node = &NetworkType{config: ntc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: networktype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: networktype.FieldID,
			},
		}
	)
	if value, ok := ntc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: networktype.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := ntc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: networktype.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := ntc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: networktype.FieldName,
		})
		_node.Name = value
	}
	if nodes := ntc.mutation.FormulaNetworkTypeFKIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   networktype.FormulaNetworkTypeFKTable,
			Columns: []string{networktype.FormulaNetworkTypeFKColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: formula.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// NetworkTypeCreateBulk is the builder for creating a bulk of NetworkType entities.
type NetworkTypeCreateBulk struct {
	config
	builders []*NetworkTypeCreate
}

// Save creates the NetworkType entities in the database.
func (ntcb *NetworkTypeCreateBulk) Save(ctx context.Context) ([]*NetworkType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ntcb.builders))
	nodes := make([]*NetworkType, len(ntcb.builders))
	mutators := make([]Mutator, len(ntcb.builders))
	for i := range ntcb.builders {
		func(i int, root context.Context) {
			builder := ntcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*NetworkTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ntcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ntcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ntcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (ntcb *NetworkTypeCreateBulk) SaveX(ctx context.Context) []*NetworkType {
	v, err := ntcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
