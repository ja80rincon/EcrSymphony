// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"math"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/counter"
	"github.com/facebookincubator/symphony/pkg/ent/counterformula"
	"github.com/facebookincubator/symphony/pkg/ent/formula"
	"github.com/facebookincubator/symphony/pkg/ent/predicate"
)

// CounterFormulaQuery is the builder for querying CounterFormula entities.
type CounterFormulaQuery struct {
	config
	limit      *int
	offset     *int
	order      []OrderFunc
	unique     []string
	predicates []predicate.CounterFormula
	// eager-loading edges.
	withFormula *FormulaQuery
	withCounter *CounterQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the builder.
func (cfq *CounterFormulaQuery) Where(ps ...predicate.CounterFormula) *CounterFormulaQuery {
	cfq.predicates = append(cfq.predicates, ps...)
	return cfq
}

// Limit adds a limit step to the query.
func (cfq *CounterFormulaQuery) Limit(limit int) *CounterFormulaQuery {
	cfq.limit = &limit
	return cfq
}

// Offset adds an offset step to the query.
func (cfq *CounterFormulaQuery) Offset(offset int) *CounterFormulaQuery {
	cfq.offset = &offset
	return cfq
}

// Order adds an order step to the query.
func (cfq *CounterFormulaQuery) Order(o ...OrderFunc) *CounterFormulaQuery {
	cfq.order = append(cfq.order, o...)
	return cfq
}

// QueryFormula chains the current query on the formula edge.
func (cfq *CounterFormulaQuery) QueryFormula() *FormulaQuery {
	query := &FormulaQuery{config: cfq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cfq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(counterformula.Table, counterformula.FieldID, selector),
			sqlgraph.To(formula.Table, formula.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, counterformula.FormulaTable, counterformula.FormulaColumn),
		)
		fromU = sqlgraph.SetNeighbors(cfq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCounter chains the current query on the counter edge.
func (cfq *CounterFormulaQuery) QueryCounter() *CounterQuery {
	query := &CounterQuery{config: cfq.config}
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cfq.sqlQuery()
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(counterformula.Table, counterformula.FieldID, selector),
			sqlgraph.To(counter.Table, counter.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, counterformula.CounterTable, counterformula.CounterColumn),
		)
		fromU = sqlgraph.SetNeighbors(cfq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CounterFormula entity in the query. Returns *NotFoundError when no counterformula was found.
func (cfq *CounterFormulaQuery) First(ctx context.Context) (*CounterFormula, error) {
	nodes, err := cfq.Limit(1).All(ctx)
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{counterformula.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cfq *CounterFormulaQuery) FirstX(ctx context.Context) *CounterFormula {
	node, err := cfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CounterFormula id in the query. Returns *NotFoundError when no id was found.
func (cfq *CounterFormulaQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(1).IDs(ctx); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{counterformula.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cfq *CounterFormulaQuery) FirstIDX(ctx context.Context) int {
	id, err := cfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns the only CounterFormula entity in the query, returns an error if not exactly one entity was returned.
func (cfq *CounterFormulaQuery) Only(ctx context.Context) (*CounterFormula, error) {
	nodes, err := cfq.Limit(2).All(ctx)
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{counterformula.Label}
	default:
		return nil, &NotSingularError{counterformula.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cfq *CounterFormulaQuery) OnlyX(ctx context.Context) *CounterFormula {
	node, err := cfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID returns the only CounterFormula id in the query, returns an error if not exactly one id was returned.
func (cfq *CounterFormulaQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = cfq.Limit(2).IDs(ctx); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = &NotSingularError{counterformula.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cfq *CounterFormulaQuery) OnlyIDX(ctx context.Context) int {
	id, err := cfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CounterFormulas.
func (cfq *CounterFormulaQuery) All(ctx context.Context) ([]*CounterFormula, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	return cfq.sqlAll(ctx)
}

// AllX is like All, but panics if an error occurs.
func (cfq *CounterFormulaQuery) AllX(ctx context.Context) []*CounterFormula {
	nodes, err := cfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CounterFormula ids.
func (cfq *CounterFormulaQuery) IDs(ctx context.Context) ([]int, error) {
	var ids []int
	if err := cfq.Select(counterformula.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cfq *CounterFormulaQuery) IDsX(ctx context.Context) []int {
	ids, err := cfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cfq *CounterFormulaQuery) Count(ctx context.Context) (int, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return cfq.sqlCount(ctx)
}

// CountX is like Count, but panics if an error occurs.
func (cfq *CounterFormulaQuery) CountX(ctx context.Context) int {
	count, err := cfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cfq *CounterFormulaQuery) Exist(ctx context.Context) (bool, error) {
	if err := cfq.prepareQuery(ctx); err != nil {
		return false, err
	}
	return cfq.sqlExist(ctx)
}

// ExistX is like Exist, but panics if an error occurs.
func (cfq *CounterFormulaQuery) ExistX(ctx context.Context) bool {
	exist, err := cfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the query builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cfq *CounterFormulaQuery) Clone() *CounterFormulaQuery {
	if cfq == nil {
		return nil
	}
	return &CounterFormulaQuery{
		config:      cfq.config,
		limit:       cfq.limit,
		offset:      cfq.offset,
		order:       append([]OrderFunc{}, cfq.order...),
		unique:      append([]string{}, cfq.unique...),
		predicates:  append([]predicate.CounterFormula{}, cfq.predicates...),
		withFormula: cfq.withFormula.Clone(),
		withCounter: cfq.withCounter.Clone(),
		// clone intermediate query.
		sql:  cfq.sql.Clone(),
		path: cfq.path,
	}
}

//  WithFormula tells the query-builder to eager-loads the nodes that are connected to
// the "formula" edge. The optional arguments used to configure the query builder of the edge.
func (cfq *CounterFormulaQuery) WithFormula(opts ...func(*FormulaQuery)) *CounterFormulaQuery {
	query := &FormulaQuery{config: cfq.config}
	for _, opt := range opts {
		opt(query)
	}
	cfq.withFormula = query
	return cfq
}

//  WithCounter tells the query-builder to eager-loads the nodes that are connected to
// the "counter" edge. The optional arguments used to configure the query builder of the edge.
func (cfq *CounterFormulaQuery) WithCounter(opts ...func(*CounterQuery)) *CounterFormulaQuery {
	query := &CounterQuery{config: cfq.config}
	for _, opt := range opts {
		opt(query)
	}
	cfq.withCounter = query
	return cfq
}

// GroupBy used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CounterFormula.Query().
//		GroupBy(counterformula.FieldCreateTime).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
//
func (cfq *CounterFormulaQuery) GroupBy(field string, fields ...string) *CounterFormulaGroupBy {
	group := &CounterFormulaGroupBy{config: cfq.config}
	group.fields = append([]string{field}, fields...)
	group.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cfq.sqlQuery(), nil
	}
	return group
}

// Select one or more fields from the given query.
//
// Example:
//
//	var v []struct {
//		CreateTime time.Time `json:"create_time,omitempty"`
//	}
//
//	client.CounterFormula.Query().
//		Select(counterformula.FieldCreateTime).
//		Scan(ctx, &v)
//
func (cfq *CounterFormulaQuery) Select(field string, fields ...string) *CounterFormulaSelect {
	selector := &CounterFormulaSelect{config: cfq.config}
	selector.fields = append([]string{field}, fields...)
	selector.path = func(ctx context.Context) (prev *sql.Selector, err error) {
		if err := cfq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		return cfq.sqlQuery(), nil
	}
	return selector
}

func (cfq *CounterFormulaQuery) prepareQuery(ctx context.Context) error {
	if cfq.path != nil {
		prev, err := cfq.path(ctx)
		if err != nil {
			return err
		}
		cfq.sql = prev
	}
	if err := counterformula.Policy.EvalQuery(ctx, cfq); err != nil {
		return err
	}
	return nil
}

func (cfq *CounterFormulaQuery) sqlAll(ctx context.Context) ([]*CounterFormula, error) {
	var (
		nodes       = []*CounterFormula{}
		withFKs     = cfq.withFKs
		_spec       = cfq.querySpec()
		loadedTypes = [2]bool{
			cfq.withFormula != nil,
			cfq.withCounter != nil,
		}
	)
	if cfq.withFormula != nil || cfq.withCounter != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, counterformula.ForeignKeys...)
	}
	_spec.ScanValues = func() []interface{} {
		node := &CounterFormula{config: cfq.config}
		nodes = append(nodes, node)
		values := node.scanValues()
		if withFKs {
			values = append(values, node.fkValues()...)
		}
		return values
	}
	_spec.Assign = func(values ...interface{}) error {
		if len(nodes) == 0 {
			return fmt.Errorf("ent: Assign called without calling ScanValues")
		}
		node := nodes[len(nodes)-1]
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(values...)
	}
	if err := sqlgraph.QueryNodes(ctx, cfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}

	if query := cfq.withFormula; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*CounterFormula)
		for i := range nodes {
			if fk := nodes[i].formula_counterformula; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(formula.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "formula_counterformula" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Formula = n
			}
		}
	}

	if query := cfq.withCounter; query != nil {
		ids := make([]int, 0, len(nodes))
		nodeids := make(map[int][]*CounterFormula)
		for i := range nodes {
			if fk := nodes[i].counter_counter_fk; fk != nil {
				ids = append(ids, *fk)
				nodeids[*fk] = append(nodeids[*fk], nodes[i])
			}
		}
		query.Where(counter.IDIn(ids...))
		neighbors, err := query.All(ctx)
		if err != nil {
			return nil, err
		}
		for _, n := range neighbors {
			nodes, ok := nodeids[n.ID]
			if !ok {
				return nil, fmt.Errorf(`unexpected foreign-key "counter_counter_fk" returned %v`, n.ID)
			}
			for i := range nodes {
				nodes[i].Edges.Counter = n
			}
		}
	}

	return nodes, nil
}

func (cfq *CounterFormulaQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cfq.querySpec()
	return sqlgraph.CountNodes(ctx, cfq.driver, _spec)
}

func (cfq *CounterFormulaQuery) sqlExist(ctx context.Context) (bool, error) {
	n, err := cfq.sqlCount(ctx)
	if err != nil {
		return false, fmt.Errorf("ent: check existence: %v", err)
	}
	return n > 0, nil
}

func (cfq *CounterFormulaQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := &sqlgraph.QuerySpec{
		Node: &sqlgraph.NodeSpec{
			Table:   counterformula.Table,
			Columns: counterformula.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: counterformula.FieldID,
			},
		},
		From:   cfq.sql,
		Unique: true,
	}
	if ps := cfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cfq.limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cfq.offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector, counterformula.ValidColumn)
			}
		}
	}
	return _spec
}

func (cfq *CounterFormulaQuery) sqlQuery() *sql.Selector {
	builder := sql.Dialect(cfq.driver.Dialect())
	t1 := builder.Table(counterformula.Table)
	selector := builder.Select(t1.Columns(counterformula.Columns...)...).From(t1)
	if cfq.sql != nil {
		selector = cfq.sql
		selector.Select(selector.Columns(counterformula.Columns...)...)
	}
	for _, p := range cfq.predicates {
		p(selector)
	}
	for _, p := range cfq.order {
		p(selector, counterformula.ValidColumn)
	}
	if offset := cfq.offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cfq.limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CounterFormulaGroupBy is the builder for group-by CounterFormula entities.
type CounterFormulaGroupBy struct {
	config
	fields []string
	fns    []AggregateFunc
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cfgb *CounterFormulaGroupBy) Aggregate(fns ...AggregateFunc) *CounterFormulaGroupBy {
	cfgb.fns = append(cfgb.fns, fns...)
	return cfgb
}

// Scan applies the group-by query and scan the result into the given value.
func (cfgb *CounterFormulaGroupBy) Scan(ctx context.Context, v interface{}) error {
	query, err := cfgb.path(ctx)
	if err != nil {
		return err
	}
	cfgb.sql = query
	return cfgb.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) ScanX(ctx context.Context, v interface{}) {
	if err := cfgb.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Strings(ctx context.Context) ([]string, error) {
	if len(cfgb.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaGroupBy.Strings is not achievable when grouping more than 1 field")
	}
	var v []string
	if err := cfgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) StringsX(ctx context.Context) []string {
	v, err := cfgb.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = cfgb.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaGroupBy.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) StringX(ctx context.Context) string {
	v, err := cfgb.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Ints(ctx context.Context) ([]int, error) {
	if len(cfgb.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaGroupBy.Ints is not achievable when grouping more than 1 field")
	}
	var v []int
	if err := cfgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) IntsX(ctx context.Context) []int {
	v, err := cfgb.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = cfgb.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaGroupBy.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) IntX(ctx context.Context) int {
	v, err := cfgb.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Float64s(ctx context.Context) ([]float64, error) {
	if len(cfgb.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaGroupBy.Float64s is not achievable when grouping more than 1 field")
	}
	var v []float64
	if err := cfgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) Float64sX(ctx context.Context) []float64 {
	v, err := cfgb.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = cfgb.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaGroupBy.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) Float64X(ctx context.Context) float64 {
	v, err := cfgb.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Bools(ctx context.Context) ([]bool, error) {
	if len(cfgb.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaGroupBy.Bools is not achievable when grouping more than 1 field")
	}
	var v []bool
	if err := cfgb.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) BoolsX(ctx context.Context) []bool {
	v, err := cfgb.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from group-by. It is only allowed when querying group-by with one field.
func (cfgb *CounterFormulaGroupBy) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = cfgb.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaGroupBy.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (cfgb *CounterFormulaGroupBy) BoolX(ctx context.Context) bool {
	v, err := cfgb.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (cfgb *CounterFormulaGroupBy) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cfgb.fields {
		if !counterformula.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for group-by", f)}
		}
	}
	selector := cfgb.sqlQuery()
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cfgb.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cfgb *CounterFormulaGroupBy) sqlQuery() *sql.Selector {
	selector := cfgb.sql
	columns := make([]string, 0, len(cfgb.fields)+len(cfgb.fns))
	columns = append(columns, cfgb.fields...)
	for _, fn := range cfgb.fns {
		columns = append(columns, fn(selector, counterformula.ValidColumn))
	}
	return selector.Select(columns...).GroupBy(cfgb.fields...)
}

// CounterFormulaSelect is the builder for select fields of CounterFormula entities.
type CounterFormulaSelect struct {
	config
	fields []string
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Scan applies the selector query and scan the result into the given value.
func (cfs *CounterFormulaSelect) Scan(ctx context.Context, v interface{}) error {
	query, err := cfs.path(ctx)
	if err != nil {
		return err
	}
	cfs.sql = query
	return cfs.sqlScan(ctx, v)
}

// ScanX is like Scan, but panics if an error occurs.
func (cfs *CounterFormulaSelect) ScanX(ctx context.Context, v interface{}) {
	if err := cfs.Scan(ctx, v); err != nil {
		panic(err)
	}
}

// Strings returns list of strings from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Strings(ctx context.Context) ([]string, error) {
	if len(cfs.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaSelect.Strings is not achievable when selecting more than 1 field")
	}
	var v []string
	if err := cfs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// StringsX is like Strings, but panics if an error occurs.
func (cfs *CounterFormulaSelect) StringsX(ctx context.Context) []string {
	v, err := cfs.Strings(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// String returns a single string from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) String(ctx context.Context) (_ string, err error) {
	var v []string
	if v, err = cfs.Strings(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaSelect.Strings returned %d results when one was expected", len(v))
	}
	return
}

// StringX is like String, but panics if an error occurs.
func (cfs *CounterFormulaSelect) StringX(ctx context.Context) string {
	v, err := cfs.String(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Ints returns list of ints from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Ints(ctx context.Context) ([]int, error) {
	if len(cfs.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaSelect.Ints is not achievable when selecting more than 1 field")
	}
	var v []int
	if err := cfs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// IntsX is like Ints, but panics if an error occurs.
func (cfs *CounterFormulaSelect) IntsX(ctx context.Context) []int {
	v, err := cfs.Ints(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Int returns a single int from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Int(ctx context.Context) (_ int, err error) {
	var v []int
	if v, err = cfs.Ints(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaSelect.Ints returned %d results when one was expected", len(v))
	}
	return
}

// IntX is like Int, but panics if an error occurs.
func (cfs *CounterFormulaSelect) IntX(ctx context.Context) int {
	v, err := cfs.Int(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64s returns list of float64s from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Float64s(ctx context.Context) ([]float64, error) {
	if len(cfs.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaSelect.Float64s is not achievable when selecting more than 1 field")
	}
	var v []float64
	if err := cfs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// Float64sX is like Float64s, but panics if an error occurs.
func (cfs *CounterFormulaSelect) Float64sX(ctx context.Context) []float64 {
	v, err := cfs.Float64s(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Float64 returns a single float64 from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Float64(ctx context.Context) (_ float64, err error) {
	var v []float64
	if v, err = cfs.Float64s(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaSelect.Float64s returned %d results when one was expected", len(v))
	}
	return
}

// Float64X is like Float64, but panics if an error occurs.
func (cfs *CounterFormulaSelect) Float64X(ctx context.Context) float64 {
	v, err := cfs.Float64(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bools returns list of bools from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Bools(ctx context.Context) ([]bool, error) {
	if len(cfs.fields) > 1 {
		return nil, errors.New("ent: CounterFormulaSelect.Bools is not achievable when selecting more than 1 field")
	}
	var v []bool
	if err := cfs.Scan(ctx, &v); err != nil {
		return nil, err
	}
	return v, nil
}

// BoolsX is like Bools, but panics if an error occurs.
func (cfs *CounterFormulaSelect) BoolsX(ctx context.Context) []bool {
	v, err := cfs.Bools(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Bool returns a single bool from selector. It is only allowed when selecting one field.
func (cfs *CounterFormulaSelect) Bool(ctx context.Context) (_ bool, err error) {
	var v []bool
	if v, err = cfs.Bools(ctx); err != nil {
		return
	}
	switch len(v) {
	case 1:
		return v[0], nil
	case 0:
		err = &NotFoundError{counterformula.Label}
	default:
		err = fmt.Errorf("ent: CounterFormulaSelect.Bools returned %d results when one was expected", len(v))
	}
	return
}

// BoolX is like Bool, but panics if an error occurs.
func (cfs *CounterFormulaSelect) BoolX(ctx context.Context) bool {
	v, err := cfs.Bool(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

func (cfs *CounterFormulaSelect) sqlScan(ctx context.Context, v interface{}) error {
	for _, f := range cfs.fields {
		if !counterformula.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("invalid field %q for selection", f)}
		}
	}
	rows := &sql.Rows{}
	query, args := cfs.sqlQuery().Query()
	if err := cfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

func (cfs *CounterFormulaSelect) sqlQuery() sql.Querier {
	selector := cfs.sql
	selector.Select(selector.Columns(cfs.fields...)...)
	return selector
}
