// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/rule"
	"github.com/facebookincubator/symphony/pkg/ent/ruletype"
)

// RuleTypeCreate is the builder for creating a RuleType entity.
type RuleTypeCreate struct {
	config
	mutation *RuleTypeMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (rtc *RuleTypeCreate) SetCreateTime(t time.Time) *RuleTypeCreate {
	rtc.mutation.SetCreateTime(t)
	return rtc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (rtc *RuleTypeCreate) SetNillableCreateTime(t *time.Time) *RuleTypeCreate {
	if t != nil {
		rtc.SetCreateTime(*t)
	}
	return rtc
}

// SetUpdateTime sets the update_time field.
func (rtc *RuleTypeCreate) SetUpdateTime(t time.Time) *RuleTypeCreate {
	rtc.mutation.SetUpdateTime(t)
	return rtc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (rtc *RuleTypeCreate) SetNillableUpdateTime(t *time.Time) *RuleTypeCreate {
	if t != nil {
		rtc.SetUpdateTime(*t)
	}
	return rtc
}

// SetName sets the name field.
func (rtc *RuleTypeCreate) SetName(s string) *RuleTypeCreate {
	rtc.mutation.SetName(s)
	return rtc
}

// AddRuletyperuleIDs adds the ruletyperule edge to Rule by ids.
func (rtc *RuleTypeCreate) AddRuletyperuleIDs(ids ...int) *RuleTypeCreate {
	rtc.mutation.AddRuletyperuleIDs(ids...)
	return rtc
}

// AddRuletyperule adds the ruletyperule edges to Rule.
func (rtc *RuleTypeCreate) AddRuletyperule(r ...*Rule) *RuleTypeCreate {
	ids := make([]int, len(r))
	for i := range r {
		ids[i] = r[i].ID
	}
	return rtc.AddRuletyperuleIDs(ids...)
}

// Mutation returns the RuleTypeMutation object of the builder.
func (rtc *RuleTypeCreate) Mutation() *RuleTypeMutation {
	return rtc.mutation
}

// Save creates the RuleType in the database.
func (rtc *RuleTypeCreate) Save(ctx context.Context) (*RuleType, error) {
	var (
		err  error
		node *RuleType
	)
	rtc.defaults()
	if len(rtc.hooks) == 0 {
		if err = rtc.check(); err != nil {
			return nil, err
		}
		node, err = rtc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*RuleTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = rtc.check(); err != nil {
				return nil, err
			}
			rtc.mutation = mutation
			node, err = rtc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(rtc.hooks) - 1; i >= 0; i-- {
			mut = rtc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, rtc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (rtc *RuleTypeCreate) SaveX(ctx context.Context) *RuleType {
	v, err := rtc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (rtc *RuleTypeCreate) defaults() {
	if _, ok := rtc.mutation.CreateTime(); !ok {
		v := ruletype.DefaultCreateTime()
		rtc.mutation.SetCreateTime(v)
	}
	if _, ok := rtc.mutation.UpdateTime(); !ok {
		v := ruletype.DefaultUpdateTime()
		rtc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (rtc *RuleTypeCreate) check() error {
	if _, ok := rtc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := rtc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := rtc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := rtc.mutation.Name(); ok {
		if err := ruletype.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (rtc *RuleTypeCreate) sqlSave(ctx context.Context) (*RuleType, error) {
	_node, _spec := rtc.createSpec()
	if err := sqlgraph.CreateNode(ctx, rtc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (rtc *RuleTypeCreate) createSpec() (*RuleType, *sqlgraph.CreateSpec) {
	var (
		_node = &RuleType{config: rtc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: ruletype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: ruletype.FieldID,
			},
		}
	)
	if value, ok := rtc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: ruletype.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := rtc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: ruletype.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := rtc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: ruletype.FieldName,
		})
		_node.Name = value
	}
	if nodes := rtc.mutation.RuletyperuleIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   ruletype.RuletyperuleTable,
			Columns: []string{ruletype.RuletyperuleColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: rule.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// RuleTypeCreateBulk is the builder for creating a bulk of RuleType entities.
type RuleTypeCreateBulk struct {
	config
	builders []*RuleTypeCreate
}

// Save creates the RuleType entities in the database.
func (rtcb *RuleTypeCreateBulk) Save(ctx context.Context) ([]*RuleType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(rtcb.builders))
	nodes := make([]*RuleType, len(rtcb.builders))
	mutators := make([]Mutator, len(rtcb.builders))
	for i := range rtcb.builders {
		func(i int, root context.Context) {
			builder := rtcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*RuleTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, rtcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, rtcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, rtcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (rtcb *RuleTypeCreateBulk) SaveX(ctx context.Context) []*RuleType {
	v, err := rtcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
