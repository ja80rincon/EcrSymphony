// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebookincubator/symphony/pkg/ent/counterfamily"
)

// CounterFamily is the model entity for the CounterFamily schema.
type CounterFamily struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CounterFamilyQuery when eager-loading is set.
	Edges CounterFamilyEdges `json:"edges"`
}

// CounterFamilyEdges holds the relations/edges for other nodes in the graph.
type CounterFamilyEdges struct {
	// Counterfamily holds the value of the counterfamily edge.
	Counterfamily []*Counter
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CounterfamilyOrErr returns the Counterfamily value or an error if the edge
// was not loaded in eager-loading.
func (e CounterFamilyEdges) CounterfamilyOrErr() ([]*Counter, error) {
	if e.loadedTypes[0] {
		return e.Counterfamily, nil
	}
	return nil, &NotLoadedError{edge: "counterfamily"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CounterFamily) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullTime{},   // create_time
		&sql.NullTime{},   // update_time
		&sql.NullString{}, // name
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CounterFamily fields.
func (cf *CounterFamily) assignValues(values ...interface{}) error {
	if m, n := len(values), len(counterfamily.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	cf.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field create_time", values[0])
	} else if value.Valid {
		cf.CreateTime = value.Time
	}
	if value, ok := values[1].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field update_time", values[1])
	} else if value.Valid {
		cf.UpdateTime = value.Time
	}
	if value, ok := values[2].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[2])
	} else if value.Valid {
		cf.Name = value.String
	}
	return nil
}

// QueryCounterfamily queries the counterfamily edge of the CounterFamily.
func (cf *CounterFamily) QueryCounterfamily() *CounterQuery {
	return (&CounterFamilyClient{config: cf.config}).QueryCounterfamily(cf)
}

// Update returns a builder for updating this CounterFamily.
// Note that, you need to call CounterFamily.Unwrap() before calling this method, if this CounterFamily
// was returned from a transaction, and the transaction was committed or rolled back.
func (cf *CounterFamily) Update() *CounterFamilyUpdateOne {
	return (&CounterFamilyClient{config: cf.config}).UpdateOne(cf)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (cf *CounterFamily) Unwrap() *CounterFamily {
	tx, ok := cf.config.driver.(*txDriver)
	if !ok {
		panic("ent: CounterFamily is not a transactional entity")
	}
	cf.config.driver = tx.drv
	return cf
}

// String implements the fmt.Stringer.
func (cf *CounterFamily) String() string {
	var builder strings.Builder
	builder.WriteString("CounterFamily(")
	builder.WriteString(fmt.Sprintf("id=%v", cf.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(cf.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(cf.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(cf.Name)
	builder.WriteByte(')')
	return builder.String()
}

// CounterFamilies is a parsable slice of CounterFamily.
type CounterFamilies []*CounterFamily

func (cf CounterFamilies) config(cfg config) {
	for _i := range cf {
		cf[_i].config = cfg
	}
}
