// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/alarmfilter"
	"github.com/facebookincubator/symphony/pkg/ent/alarmstatus"
)

// AlarmStatusCreate is the builder for creating a AlarmStatus entity.
type AlarmStatusCreate struct {
	config
	mutation *AlarmStatusMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (asc *AlarmStatusCreate) SetCreateTime(t time.Time) *AlarmStatusCreate {
	asc.mutation.SetCreateTime(t)
	return asc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (asc *AlarmStatusCreate) SetNillableCreateTime(t *time.Time) *AlarmStatusCreate {
	if t != nil {
		asc.SetCreateTime(*t)
	}
	return asc
}

// SetUpdateTime sets the update_time field.
func (asc *AlarmStatusCreate) SetUpdateTime(t time.Time) *AlarmStatusCreate {
	asc.mutation.SetUpdateTime(t)
	return asc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (asc *AlarmStatusCreate) SetNillableUpdateTime(t *time.Time) *AlarmStatusCreate {
	if t != nil {
		asc.SetUpdateTime(*t)
	}
	return asc
}

// SetName sets the name field.
func (asc *AlarmStatusCreate) SetName(s string) *AlarmStatusCreate {
	asc.mutation.SetName(s)
	return asc
}

// AddAlarmStatusFkIDs adds the alarmStatusFk edge to AlarmFilter by ids.
func (asc *AlarmStatusCreate) AddAlarmStatusFkIDs(ids ...int) *AlarmStatusCreate {
	asc.mutation.AddAlarmStatusFkIDs(ids...)
	return asc
}

// AddAlarmStatusFk adds the alarmStatusFk edges to AlarmFilter.
func (asc *AlarmStatusCreate) AddAlarmStatusFk(a ...*AlarmFilter) *AlarmStatusCreate {
	ids := make([]int, len(a))
	for i := range a {
		ids[i] = a[i].ID
	}
	return asc.AddAlarmStatusFkIDs(ids...)
}

// Mutation returns the AlarmStatusMutation object of the builder.
func (asc *AlarmStatusCreate) Mutation() *AlarmStatusMutation {
	return asc.mutation
}

// Save creates the AlarmStatus in the database.
func (asc *AlarmStatusCreate) Save(ctx context.Context) (*AlarmStatus, error) {
	var (
		err  error
		node *AlarmStatus
	)
	asc.defaults()
	if len(asc.hooks) == 0 {
		if err = asc.check(); err != nil {
			return nil, err
		}
		node, err = asc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*AlarmStatusMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = asc.check(); err != nil {
				return nil, err
			}
			asc.mutation = mutation
			node, err = asc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(asc.hooks) - 1; i >= 0; i-- {
			mut = asc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, asc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (asc *AlarmStatusCreate) SaveX(ctx context.Context) *AlarmStatus {
	v, err := asc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (asc *AlarmStatusCreate) defaults() {
	if _, ok := asc.mutation.CreateTime(); !ok {
		v := alarmstatus.DefaultCreateTime()
		asc.mutation.SetCreateTime(v)
	}
	if _, ok := asc.mutation.UpdateTime(); !ok {
		v := alarmstatus.DefaultUpdateTime()
		asc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (asc *AlarmStatusCreate) check() error {
	if _, ok := asc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := asc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := asc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := asc.mutation.Name(); ok {
		if err := alarmstatus.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (asc *AlarmStatusCreate) sqlSave(ctx context.Context) (*AlarmStatus, error) {
	_node, _spec := asc.createSpec()
	if err := sqlgraph.CreateNode(ctx, asc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (asc *AlarmStatusCreate) createSpec() (*AlarmStatus, *sqlgraph.CreateSpec) {
	var (
		_node = &AlarmStatus{config: asc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: alarmstatus.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: alarmstatus.FieldID,
			},
		}
	)
	if value, ok := asc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: alarmstatus.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := asc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: alarmstatus.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := asc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: alarmstatus.FieldName,
		})
		_node.Name = value
	}
	if nodes := asc.mutation.AlarmStatusFkIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   alarmstatus.AlarmStatusFkTable,
			Columns: []string{alarmstatus.AlarmStatusFkColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: alarmfilter.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// AlarmStatusCreateBulk is the builder for creating a bulk of AlarmStatus entities.
type AlarmStatusCreateBulk struct {
	config
	builders []*AlarmStatusCreate
}

// Save creates the AlarmStatus entities in the database.
func (ascb *AlarmStatusCreateBulk) Save(ctx context.Context) ([]*AlarmStatus, error) {
	specs := make([]*sqlgraph.CreateSpec, len(ascb.builders))
	nodes := make([]*AlarmStatus, len(ascb.builders))
	mutators := make([]Mutator, len(ascb.builders))
	for i := range ascb.builders {
		func(i int, root context.Context) {
			builder := ascb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*AlarmStatusMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, ascb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, ascb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, ascb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (ascb *AlarmStatusCreateBulk) SaveX(ctx context.Context) []*AlarmStatus {
	v, err := ascb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
