// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/kqi"
	"github.com/facebookincubator/symphony/pkg/ent/kqisource"
)

// KqiSourceCreate is the builder for creating a KqiSource entity.
type KqiSourceCreate struct {
	config
	mutation *KqiSourceMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (ksc *KqiSourceCreate) SetCreateTime(t time.Time) *KqiSourceCreate {
	ksc.mutation.SetCreateTime(t)
	return ksc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (ksc *KqiSourceCreate) SetNillableCreateTime(t *time.Time) *KqiSourceCreate {
	if t != nil {
		ksc.SetCreateTime(*t)
	}
	return ksc
}

// SetUpdateTime sets the update_time field.
func (ksc *KqiSourceCreate) SetUpdateTime(t time.Time) *KqiSourceCreate {
	ksc.mutation.SetUpdateTime(t)
	return ksc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (ksc *KqiSourceCreate) SetNillableUpdateTime(t *time.Time) *KqiSourceCreate {
	if t != nil {
		ksc.SetUpdateTime(*t)
	}
	return ksc
}

// SetName sets the name field.
func (ksc *KqiSourceCreate) SetName(s string) *KqiSourceCreate {
	ksc.mutation.SetName(s)
	return ksc
}

// AddKqiSourceFkIDs adds the kqiSourceFk edge to Kqi by ids.
func (ksc *KqiSourceCreate) AddKqiSourceFkIDs(ids ...int) *KqiSourceCreate {
	ksc.mutation.AddKqiSourceFkIDs(ids...)
	return ksc
}

// AddKqiSourceFk adds the kqiSourceFk edges to Kqi.
func (ksc *KqiSourceCreate) AddKqiSourceFk(k ...*Kqi) *KqiSourceCreate {
	ids := make([]int, len(k))
	for i := range k {
		ids[i] = k[i].ID
	}
	return ksc.AddKqiSourceFkIDs(ids...)
}

// Mutation returns the KqiSourceMutation object of the builder.
func (ksc *KqiSourceCreate) Mutation() *KqiSourceMutation {
	return ksc.mutation
}

// Save creates the KqiSource in the database.
func (ksc *KqiSourceCreate) Save(ctx context.Context) (*KqiSource, error) {
	var (
		err  error
		node *KqiSource
	)
	ksc.defaults()
	if len(ksc.hooks) == 0 {
		if err = ksc.check(); err != nil {
			return nil, err
		}
		node, err = ksc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*KqiSourceMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = ksc.check(); err != nil {
				return nil, err
			}
			ksc.mutation = mutation
			node, err = ksc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ksc.hooks) - 1; i >= 0; i-- {
			mut = ksc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ksc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (ksc *KqiSourceCreate) SaveX(ctx context.Context) *KqiSource {
	v, err := ksc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (ksc *KqiSourceCreate) defaults() {
	if _, ok := ksc.mutation.CreateTime(); !ok {
		v := kqisource.DefaultCreateTime()
		ksc.mutation.SetCreateTime(v)
	}
	if _, ok := ksc.mutation.UpdateTime(); !ok {
		v := kqisource.DefaultUpdateTime()
		ksc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (ksc *KqiSourceCreate) check() error {
	if _, ok := ksc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := ksc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := ksc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := ksc.mutation.Name(); ok {
		if err := kqisource.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (ksc *KqiSourceCreate) sqlSave(ctx context.Context) (*KqiSource, error) {
	_node, _spec := ksc.createSpec()
	if err := sqlgraph.CreateNode(ctx, ksc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (ksc *KqiSourceCreate) createSpec() (*KqiSource, *sqlgraph.CreateSpec) {
	var (
		_node = &KqiSource{config: ksc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: kqisource.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: kqisource.FieldID,
			},
		}
	)
	if value, ok := ksc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: kqisource.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := ksc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: kqisource.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := ksc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: kqisource.FieldName,
		})
		_node.Name = value
	}
	if nodes := ksc.mutation.KqiSourceFkIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   kqisource.KqiSourceFkTable,
			Columns: []string{kqisource.KqiSourceFkColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: kqi.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// KqiSourceCreateBulk is the builder for creating a bulk of KqiSource entities.
type KqiSourceCreateBulk struct {
	config
	builders []*KqiSourceCreate
}

// Save creates the KqiSource entities in the database.
func (kscb *KqiSourceCreateBulk) Save(ctx context.Context) ([]*KqiSource, error) {
	specs := make([]*sqlgraph.CreateSpec, len(kscb.builders))
	nodes := make([]*KqiSource, len(kscb.builders))
	mutators := make([]Mutator, len(kscb.builders))
	for i := range kscb.builders {
		func(i int, root context.Context) {
			builder := kscb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*KqiSourceMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, kscb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, kscb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, kscb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (kscb *KqiSourceCreateBulk) SaveX(ctx context.Context) []*KqiSource {
	v, err := kscb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
