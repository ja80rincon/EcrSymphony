// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebookincubator/symphony/pkg/ent/alarmstatus"
)

// AlarmStatus is the model entity for the AlarmStatus schema.
type AlarmStatus struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the AlarmStatusQuery when eager-loading is set.
	Edges AlarmStatusEdges `json:"edges"`
}

// AlarmStatusEdges holds the relations/edges for other nodes in the graph.
type AlarmStatusEdges struct {
	// AlarmStatusFk holds the value of the alarmStatusFk edge.
	AlarmStatusFk []*AlarmFilter
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AlarmStatusFkOrErr returns the AlarmStatusFk value or an error if the edge
// was not loaded in eager-loading.
func (e AlarmStatusEdges) AlarmStatusFkOrErr() ([]*AlarmFilter, error) {
	if e.loadedTypes[0] {
		return e.AlarmStatusFk, nil
	}
	return nil, &NotLoadedError{edge: "alarmStatusFk"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AlarmStatus) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullTime{},   // create_time
		&sql.NullTime{},   // update_time
		&sql.NullString{}, // name
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AlarmStatus fields.
func (as *AlarmStatus) assignValues(values ...interface{}) error {
	if m, n := len(values), len(alarmstatus.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	as.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field create_time", values[0])
	} else if value.Valid {
		as.CreateTime = value.Time
	}
	if value, ok := values[1].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field update_time", values[1])
	} else if value.Valid {
		as.UpdateTime = value.Time
	}
	if value, ok := values[2].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[2])
	} else if value.Valid {
		as.Name = value.String
	}
	return nil
}

// QueryAlarmStatusFk queries the alarmStatusFk edge of the AlarmStatus.
func (as *AlarmStatus) QueryAlarmStatusFk() *AlarmFilterQuery {
	return (&AlarmStatusClient{config: as.config}).QueryAlarmStatusFk(as)
}

// Update returns a builder for updating this AlarmStatus.
// Note that, you need to call AlarmStatus.Unwrap() before calling this method, if this AlarmStatus
// was returned from a transaction, and the transaction was committed or rolled back.
func (as *AlarmStatus) Update() *AlarmStatusUpdateOne {
	return (&AlarmStatusClient{config: as.config}).UpdateOne(as)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (as *AlarmStatus) Unwrap() *AlarmStatus {
	tx, ok := as.config.driver.(*txDriver)
	if !ok {
		panic("ent: AlarmStatus is not a transactional entity")
	}
	as.config.driver = tx.drv
	return as
}

// String implements the fmt.Stringer.
func (as *AlarmStatus) String() string {
	var builder strings.Builder
	builder.WriteString("AlarmStatus(")
	builder.WriteString(fmt.Sprintf("id=%v", as.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(as.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(as.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(as.Name)
	builder.WriteByte(')')
	return builder.String()
}

// AlarmStatusSlice is a parsable slice of AlarmStatus.
type AlarmStatusSlice []*AlarmStatus

func (as AlarmStatusSlice) config(cfg config) {
	for _i := range as {
		as[_i].config = cfg
	}
}
