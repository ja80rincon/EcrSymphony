// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/eventseverity"
	"github.com/facebookincubator/symphony/pkg/ent/rule"
)

// EventSeverityCreate is the builder for creating a EventSeverity entity.
type EventSeverityCreate struct {
	config
	mutation *EventSeverityMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (esc *EventSeverityCreate) SetCreateTime(t time.Time) *EventSeverityCreate {
	esc.mutation.SetCreateTime(t)
	return esc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (esc *EventSeverityCreate) SetNillableCreateTime(t *time.Time) *EventSeverityCreate {
	if t != nil {
		esc.SetCreateTime(*t)
	}
	return esc
}

// SetUpdateTime sets the update_time field.
func (esc *EventSeverityCreate) SetUpdateTime(t time.Time) *EventSeverityCreate {
	esc.mutation.SetUpdateTime(t)
	return esc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (esc *EventSeverityCreate) SetNillableUpdateTime(t *time.Time) *EventSeverityCreate {
	if t != nil {
		esc.SetUpdateTime(*t)
	}
	return esc
}

// SetName sets the name field.
func (esc *EventSeverityCreate) SetName(s string) *EventSeverityCreate {
	esc.mutation.SetName(s)
	return esc
}

// AddEventseverityruleIDs adds the eventseverityrule edge to Rule by ids.
func (esc *EventSeverityCreate) AddEventseverityruleIDs(ids ...int) *EventSeverityCreate {
	esc.mutation.AddEventseverityruleIDs(ids...)
	return esc
}

// AddEventseverityrule adds the eventseverityrule edges to Rule.
func (esc *EventSeverityCreate) AddEventseverityrule(r ...*Rule) *EventSeverityCreate {
	ids := make([]int, len(r))
	for i := range r {
		ids[i] = r[i].ID
	}
	return esc.AddEventseverityruleIDs(ids...)
}

// Mutation returns the EventSeverityMutation object of the builder.
func (esc *EventSeverityCreate) Mutation() *EventSeverityMutation {
	return esc.mutation
}

// Save creates the EventSeverity in the database.
func (esc *EventSeverityCreate) Save(ctx context.Context) (*EventSeverity, error) {
	var (
		err  error
		node *EventSeverity
	)
	esc.defaults()
	if len(esc.hooks) == 0 {
		if err = esc.check(); err != nil {
			return nil, err
		}
		node, err = esc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*EventSeverityMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = esc.check(); err != nil {
				return nil, err
			}
			esc.mutation = mutation
			node, err = esc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(esc.hooks) - 1; i >= 0; i-- {
			mut = esc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, esc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (esc *EventSeverityCreate) SaveX(ctx context.Context) *EventSeverity {
	v, err := esc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (esc *EventSeverityCreate) defaults() {
	if _, ok := esc.mutation.CreateTime(); !ok {
		v := eventseverity.DefaultCreateTime()
		esc.mutation.SetCreateTime(v)
	}
	if _, ok := esc.mutation.UpdateTime(); !ok {
		v := eventseverity.DefaultUpdateTime()
		esc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (esc *EventSeverityCreate) check() error {
	if _, ok := esc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := esc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := esc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	if v, ok := esc.mutation.Name(); ok {
		if err := eventseverity.NameValidator(v); err != nil {
			return &ValidationError{Name: "name", err: fmt.Errorf("ent: validator failed for field \"name\": %w", err)}
		}
	}
	return nil
}

func (esc *EventSeverityCreate) sqlSave(ctx context.Context) (*EventSeverity, error) {
	_node, _spec := esc.createSpec()
	if err := sqlgraph.CreateNode(ctx, esc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (esc *EventSeverityCreate) createSpec() (*EventSeverity, *sqlgraph.CreateSpec) {
	var (
		_node = &EventSeverity{config: esc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: eventseverity.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: eventseverity.FieldID,
			},
		}
	)
	if value, ok := esc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: eventseverity.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := esc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: eventseverity.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := esc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: eventseverity.FieldName,
		})
		_node.Name = value
	}
	if nodes := esc.mutation.EventseverityruleIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   eventseverity.EventseverityruleTable,
			Columns: []string{eventseverity.EventseverityruleColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: rule.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// EventSeverityCreateBulk is the builder for creating a bulk of EventSeverity entities.
type EventSeverityCreateBulk struct {
	config
	builders []*EventSeverityCreate
}

// Save creates the EventSeverity entities in the database.
func (escb *EventSeverityCreateBulk) Save(ctx context.Context) ([]*EventSeverity, error) {
	specs := make([]*sqlgraph.CreateSpec, len(escb.builders))
	nodes := make([]*EventSeverity, len(escb.builders))
	mutators := make([]Mutator, len(escb.builders))
	for i := range escb.builders {
		func(i int, root context.Context) {
			builder := escb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*EventSeverityMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, escb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, escb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, escb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (escb *EventSeverityCreateBulk) SaveX(ctx context.Context) []*EventSeverity {
	v, err := escb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
