// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"github.com/facebook/ent/dialect/sql/sqlgraph"
	"github.com/facebook/ent/schema/field"
	"github.com/facebookincubator/symphony/pkg/ent/propertytype"
	"github.com/facebookincubator/symphony/pkg/ent/workertype"
)

// WorkerTypeCreate is the builder for creating a WorkerType entity.
type WorkerTypeCreate struct {
	config
	mutation *WorkerTypeMutation
	hooks    []Hook
}

// SetCreateTime sets the create_time field.
func (wtc *WorkerTypeCreate) SetCreateTime(t time.Time) *WorkerTypeCreate {
	wtc.mutation.SetCreateTime(t)
	return wtc
}

// SetNillableCreateTime sets the create_time field if the given value is not nil.
func (wtc *WorkerTypeCreate) SetNillableCreateTime(t *time.Time) *WorkerTypeCreate {
	if t != nil {
		wtc.SetCreateTime(*t)
	}
	return wtc
}

// SetUpdateTime sets the update_time field.
func (wtc *WorkerTypeCreate) SetUpdateTime(t time.Time) *WorkerTypeCreate {
	wtc.mutation.SetUpdateTime(t)
	return wtc
}

// SetNillableUpdateTime sets the update_time field if the given value is not nil.
func (wtc *WorkerTypeCreate) SetNillableUpdateTime(t *time.Time) *WorkerTypeCreate {
	if t != nil {
		wtc.SetUpdateTime(*t)
	}
	return wtc
}

// SetName sets the name field.
func (wtc *WorkerTypeCreate) SetName(s string) *WorkerTypeCreate {
	wtc.mutation.SetName(s)
	return wtc
}

// SetDescription sets the description field.
func (wtc *WorkerTypeCreate) SetDescription(s string) *WorkerTypeCreate {
	wtc.mutation.SetDescription(s)
	return wtc
}

// SetNillableDescription sets the description field if the given value is not nil.
func (wtc *WorkerTypeCreate) SetNillableDescription(s *string) *WorkerTypeCreate {
	if s != nil {
		wtc.SetDescription(*s)
	}
	return wtc
}

// AddPropertyTypeIDs adds the property_types edge to PropertyType by ids.
func (wtc *WorkerTypeCreate) AddPropertyTypeIDs(ids ...int) *WorkerTypeCreate {
	wtc.mutation.AddPropertyTypeIDs(ids...)
	return wtc
}

// AddPropertyTypes adds the property_types edges to PropertyType.
func (wtc *WorkerTypeCreate) AddPropertyTypes(p ...*PropertyType) *WorkerTypeCreate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return wtc.AddPropertyTypeIDs(ids...)
}

// Mutation returns the WorkerTypeMutation object of the builder.
func (wtc *WorkerTypeCreate) Mutation() *WorkerTypeMutation {
	return wtc.mutation
}

// Save creates the WorkerType in the database.
func (wtc *WorkerTypeCreate) Save(ctx context.Context) (*WorkerType, error) {
	var (
		err  error
		node *WorkerType
	)
	wtc.defaults()
	if len(wtc.hooks) == 0 {
		if err = wtc.check(); err != nil {
			return nil, err
		}
		node, err = wtc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*WorkerTypeMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = wtc.check(); err != nil {
				return nil, err
			}
			wtc.mutation = mutation
			node, err = wtc.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(wtc.hooks) - 1; i >= 0; i-- {
			mut = wtc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, wtc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (wtc *WorkerTypeCreate) SaveX(ctx context.Context) *WorkerType {
	v, err := wtc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// defaults sets the default values of the builder before save.
func (wtc *WorkerTypeCreate) defaults() {
	if _, ok := wtc.mutation.CreateTime(); !ok {
		v := workertype.DefaultCreateTime()
		wtc.mutation.SetCreateTime(v)
	}
	if _, ok := wtc.mutation.UpdateTime(); !ok {
		v := workertype.DefaultUpdateTime()
		wtc.mutation.SetUpdateTime(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (wtc *WorkerTypeCreate) check() error {
	if _, ok := wtc.mutation.CreateTime(); !ok {
		return &ValidationError{Name: "create_time", err: errors.New("ent: missing required field \"create_time\"")}
	}
	if _, ok := wtc.mutation.UpdateTime(); !ok {
		return &ValidationError{Name: "update_time", err: errors.New("ent: missing required field \"update_time\"")}
	}
	if _, ok := wtc.mutation.Name(); !ok {
		return &ValidationError{Name: "name", err: errors.New("ent: missing required field \"name\"")}
	}
	return nil
}

func (wtc *WorkerTypeCreate) sqlSave(ctx context.Context) (*WorkerType, error) {
	_node, _spec := wtc.createSpec()
	if err := sqlgraph.CreateNode(ctx, wtc.driver, _spec); err != nil {
		if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (wtc *WorkerTypeCreate) createSpec() (*WorkerType, *sqlgraph.CreateSpec) {
	var (
		_node = &WorkerType{config: wtc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: workertype.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: workertype.FieldID,
			},
		}
	)
	if value, ok := wtc.mutation.CreateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: workertype.FieldCreateTime,
		})
		_node.CreateTime = value
	}
	if value, ok := wtc.mutation.UpdateTime(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: workertype.FieldUpdateTime,
		})
		_node.UpdateTime = value
	}
	if value, ok := wtc.mutation.Name(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: workertype.FieldName,
		})
		_node.Name = value
	}
	if value, ok := wtc.mutation.Description(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: workertype.FieldDescription,
		})
		_node.Description = &value
	}
	if nodes := wtc.mutation.PropertyTypesIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   workertype.PropertyTypesTable,
			Columns: []string{workertype.PropertyTypesColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: propertytype.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// WorkerTypeCreateBulk is the builder for creating a bulk of WorkerType entities.
type WorkerTypeCreateBulk struct {
	config
	builders []*WorkerTypeCreate
}

// Save creates the WorkerType entities in the database.
func (wtcb *WorkerTypeCreateBulk) Save(ctx context.Context) ([]*WorkerType, error) {
	specs := make([]*sqlgraph.CreateSpec, len(wtcb.builders))
	nodes := make([]*WorkerType, len(wtcb.builders))
	mutators := make([]Mutator, len(wtcb.builders))
	for i := range wtcb.builders {
		func(i int, root context.Context) {
			builder := wtcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*WorkerTypeMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, wtcb.builders[i+1].mutation)
				} else {
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, wtcb.driver, &sqlgraph.BatchCreateSpec{Nodes: specs}); err != nil {
						if cerr, ok := isSQLConstraintError(err); ok {
							err = cerr
						}
					}
				}
				mutation.done = true
				if err != nil {
					return nil, err
				}
				id := specs[i].ID.Value.(int64)
				nodes[i].ID = int(id)
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, wtcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX calls Save and panics if Save returns an error.
func (wtcb *WorkerTypeCreateBulk) SaveX(ctx context.Context) []*WorkerType {
	v, err := wtcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}
