// Copyright (c) 2004-present Facebook All rights reserved.
// Use of this source code is governed by a BSD-style
// license that can be found in the LICENSE file.

// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"github.com/facebook/ent/dialect/sql"
	"github.com/facebookincubator/symphony/pkg/ent/ruletype"
)

// RuleType is the model entity for the RuleType schema.
type RuleType struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RuleTypeQuery when eager-loading is set.
	Edges RuleTypeEdges `json:"edges"`
}

// RuleTypeEdges holds the relations/edges for other nodes in the graph.
type RuleTypeEdges struct {
	// Ruletyperule holds the value of the ruletyperule edge.
	Ruletyperule []*Rule
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// RuletyperuleOrErr returns the Ruletyperule value or an error if the edge
// was not loaded in eager-loading.
func (e RuleTypeEdges) RuletyperuleOrErr() ([]*Rule, error) {
	if e.loadedTypes[0] {
		return e.Ruletyperule, nil
	}
	return nil, &NotLoadedError{edge: "ruletyperule"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*RuleType) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullTime{},   // create_time
		&sql.NullTime{},   // update_time
		&sql.NullString{}, // name
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the RuleType fields.
func (rt *RuleType) assignValues(values ...interface{}) error {
	if m, n := len(values), len(ruletype.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	rt.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field create_time", values[0])
	} else if value.Valid {
		rt.CreateTime = value.Time
	}
	if value, ok := values[1].(*sql.NullTime); !ok {
		return fmt.Errorf("unexpected type %T for field update_time", values[1])
	} else if value.Valid {
		rt.UpdateTime = value.Time
	}
	if value, ok := values[2].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field name", values[2])
	} else if value.Valid {
		rt.Name = value.String
	}
	return nil
}

// QueryRuletyperule queries the ruletyperule edge of the RuleType.
func (rt *RuleType) QueryRuletyperule() *RuleQuery {
	return (&RuleTypeClient{config: rt.config}).QueryRuletyperule(rt)
}

// Update returns a builder for updating this RuleType.
// Note that, you need to call RuleType.Unwrap() before calling this method, if this RuleType
// was returned from a transaction, and the transaction was committed or rolled back.
func (rt *RuleType) Update() *RuleTypeUpdateOne {
	return (&RuleTypeClient{config: rt.config}).UpdateOne(rt)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (rt *RuleType) Unwrap() *RuleType {
	tx, ok := rt.config.driver.(*txDriver)
	if !ok {
		panic("ent: RuleType is not a transactional entity")
	}
	rt.config.driver = tx.drv
	return rt
}

// String implements the fmt.Stringer.
func (rt *RuleType) String() string {
	var builder strings.Builder
	builder.WriteString("RuleType(")
	builder.WriteString(fmt.Sprintf("id=%v", rt.ID))
	builder.WriteString(", create_time=")
	builder.WriteString(rt.CreateTime.Format(time.ANSIC))
	builder.WriteString(", update_time=")
	builder.WriteString(rt.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", name=")
	builder.WriteString(rt.Name)
	builder.WriteByte(')')
	return builder.String()
}

// RuleTypes is a parsable slice of RuleType.
type RuleTypes []*RuleType

func (rt RuleTypes) config(cfg config) {
	for _i := range rt {
		rt[_i].config = cfg
	}
}
